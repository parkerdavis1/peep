{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["@sveltejs/opencode"],
  "agent": {
    "build": {
      "permission": {
        "git commit *": "ask",
        "git push *": "deny"
      }
    }
  }
}
